import {query} from '../lib/db.js';
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const r=await query(`select status,count(*)::int count,coalesce(sum(amount_usd),0) amount from donations group by status`);const donors=await query(`select count(*)::int count from donors`);const projects=await query(`select count(*)::int count from projects where status<>'Archived'`);return res.status(200).json({donors:donors.rows[0].count,projects:projects.rows[0].count,donations:r.rows});}catch(e){console.error(e);return res.status(500).json({error:'Dashboard unavailable.'});}}