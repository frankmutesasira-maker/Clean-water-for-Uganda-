export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const ref=String(req.query.reference||'').trim();if(!/^CWU-\d{4}-[A-Z0-9]{8}$/.test(ref))return res.status(400).json({status:'INVALID',message:'Enter a valid donation reference.'});/* Production: query PostgreSQL by reference. */return res.status(200).json({status:'NOT_FOUND',message:'No donation record was found for this reference.'});}