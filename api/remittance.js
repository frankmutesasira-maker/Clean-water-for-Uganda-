export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const b=req.body||{};if(!b.reference||!b.amount||!b.transfer_reference)return res.status(400).json({error:'Donation reference, amount and transfer reference are required.'});/* Production: store remittance details securely and queue finance verification. */return res.status(201).json({status:'PENDING',reference:b.reference,message:'Remittance submitted for finance review.'});}