export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const b=req.body||{};if(!b.reference||!b.amount||!b.transfer_reference||!b.transfer_date)return res.status(400).json({error:'Reference, amount, transfer reference and transfer date are required.'});/* Production: attach transfer to donation and set finance review state to PENDING. */return res.status(201).json({status:'PENDING',reference:b.reference,message:'Transfer submitted for finance review.'});}