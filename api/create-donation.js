export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const b=req.body||{};for(const k of ['first_name','last_name','email','country','amount','frequency','project','payment_method'])if(!b[k])return res.status(400).json({error:`Missing ${k}`});if(Number(b.amount)<=0)return res.status(400).json({error:'Invalid amount'});const reference=`CWU-${new Date().getFullYear()}-${crypto.randomUUID().replaceAll('-','').slice(0,8).toUpperCase()}`;/* Production: insert donor + donation in PostgreSQL as PENDING. */return res.status(201).json({reference,status:'PENDING',message:`Donation created. Your reference is ${reference}.`});}